from setuptools import find_packages, setup

setup(
    name='attendance',
    version='1.0.0a',
    packages=find_packages(),
    include_package_data=True,
    zip_safe=False,
    data_files=[('config', ['src/config/logging.yaml', 
                            'src/config/config.py', 
                            'src/config/attendance.service', 
                            'src/config/attendance'])],
    install_requires=['flask', 
                        'flask-sqlalchemy', 
                        'flask-wtf[email]', 
                        'pandas',
                        'xlrd', 
                        'pyyaml', 
                        'gunicorn']
)